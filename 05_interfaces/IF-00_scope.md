Interface Scope

Interfaces define the permissible points of interaction
between the system, the human (H), and external contexts.

No interface may introduce autonomy, optimization,
or hidden state transitions that violate system invariants.
