Security Scope

Security in RAMORGA is invariant-driven.
Threats are defined as violations of system invariants,
not as external attack vectors.

Each security control maps directly to one or more invariants.
