Architecture Test Scope

Architecture tests validate compliance with system invariants.
They do not test implementation details, performance, or behavior,
but verify that architectural decisions remain within constitutional bounds.

Each test maps directly to a single invariant (INV-xx).
